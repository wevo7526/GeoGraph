# China / Taiwan Strait pack — Phase 6

The flagship second region (build-spec decision 8). The seven contract files
are here and `core.packs.available()` lists it; boot seeds it alongside MENA.

The P6 exit test was that the core runs against these files UNCHANGED — no
`core/` diff accompanied this pack's landing, which is the contract holding.
Region-specific notes live where they bind:

- `assets.yaml` records the ABSENCE of 13F-visible Chinese sovereign filers —
  a finding, not a gap.
- `marquee_events.yaml` explains the 046 coding for the Nixon and Pelosi
  visits and why the curated spine leans post-2014 (COW MIDs carry the
  earlier conflicts).
- Shared instruments (^GSPC, gold, Brent, DGS10) reuse the same node ids as
  the MENA pack — one market, one node.
